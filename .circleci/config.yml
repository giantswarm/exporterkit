version: 2.1

orbs:
  architect: giantswarm/architect@9.4.2

workflows:
  test:
    jobs:
      - architect/go-test:
          context: architect
          name: go-test
          filters:
            # Trigger job also on git tag.
            tags:
              only: /^v.*/
